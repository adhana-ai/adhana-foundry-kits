# Nothing. Not "nothing yet" -- nothing.
#
# The database is sqlite3, the HTTP client is urllib, the server is http.server and the UI is three
# static files. All of that is the Python standard library, so the fork test is `git clone` then
# `python3 -m src.app`, with no install step at all.
#
# UC001 needs one dependency (pypdf) because it extracts text from PDFs. This kit reads a schema
# and runs a query, and Python already does both.
