# NOTHING. This kit is Python standard library, end to end.
#
# The corpus is generated in-process (tools/build_corpus.py, seeded, no network), history and
# request records are plain JSONL, and the one HTTP call this kit ever makes goes through urllib
# in src/adapters/__init__.py. There was nothing to add.
