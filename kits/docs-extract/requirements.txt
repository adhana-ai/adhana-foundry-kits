# NOTHING. This kit is Python standard library, end to end.
#
# It was not written to be minimal for its own sake -- docs-qa has one dependency, pypdf, and
# that dependency earns its place because PDF text extraction is genuinely not something to
# hand-roll. This corpus is plain text fetched over urllib, so there was nothing left to add.
#
# ⚠︎ THIS FILE WAS COPIED FROM docs-qa AND LISTED pypdf FOR A WHILE. Nothing imports it here.
# A requirements file naming a package the code never touches is a small lie that costs a
# forker an install and teaches them the kit is bigger than it is.
