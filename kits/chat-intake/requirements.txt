# NOTHING. This kit is Python standard library, end to end.
#
# The corpus arrives over urllib, the dialogue state is JSON, and the grader is a set comparison —
# there was nothing left to add. A requirements file naming a package the code never imports costs
# a forker an install and teaches them the kit is bigger than it is.
