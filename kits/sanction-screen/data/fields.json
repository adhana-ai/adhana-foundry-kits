{
 "fields": [
  {"name": "alert_id", "type": "string",
   "hint": "the alert reference, verbatim"},
  {"name": "customer_name", "type": "string",
   "hint": "the name on the CUSTOMER RECORD, verbatim"},
  {"name": "listed_name", "type": "string",
   "hint": "the PRIMARY listed name on the WATCHLIST ENTRY, verbatim -- the 'Listed Name' line, never an alias from the 'Also Listed As' line"},
  {"name": "customer_identifier_type", "type": "enum",
   "values": ["passport_number", "national_id_number", "tax_reference", "none"],
   "hint": "which strong identifier the CUSTOMER RECORD carries. Return 'none' when the record says no secondary identifier is on file -- 'none' is a value, never a blank"},
  {"name": "customer_identifier_value", "type": "string",
   "hint": "the CUSTOMER RECORD's value for that identifier, verbatim. Return null when customer_identifier_type is 'none'"},
  {"name": "listed_identifier_type", "type": "enum",
   "values": ["passport_number", "national_id_number", "tax_reference", "none"],
   "hint": "which strong identifier the WATCHLIST ENTRY carries. Return 'none' when the entry says no identifier is published -- it may be a DIFFERENT type from the customer record's, and that is a real and common state"},
  {"name": "listed_identifier_value", "type": "string",
   "hint": "the WATCHLIST ENTRY's value for that identifier, verbatim. Return null when listed_identifier_type is 'none'"},
  {"name": "customer_dob", "type": "string",
   "hint": "the CUSTOMER RECORD's date of birth EXACTLY as stated, including a partial one -- copy '1978' or '1978-04' as it stands and do not pad it. Return null only where the record says the date of birth is not recorded"},
  {"name": "listed_dob", "type": "string",
   "hint": "the WATCHLIST ENTRY's date of birth EXACTLY as stated, including a partial one. Return null only where the entry says no date of birth is published"},
  {"name": "customer_place_of_birth", "type": "string",
   "hint": "the CUSTOMER RECORD's place of birth, verbatim. Return null where the record says it is not recorded"},
  {"name": "listed_place_of_birth", "type": "string",
   "hint": "the WATCHLIST ENTRY's place of birth, verbatim. Return null where the entry says none is published"},
  {"name": "customer_nationality", "type": "string",
   "hint": "the nationality on the CUSTOMER RECORD, verbatim. Report it as stated -- it is NOT an input to the verdict"},
  {"name": "listed_nationality", "type": "string",
   "hint": "the nationality on the WATCHLIST ENTRY, verbatim. Report it as stated -- it is NOT an input to the verdict, and a list record's nationality is often stale or secondary"},
  {"name": "account_status", "type": "enum", "values": ["pending_onboarding", "live"],
   "hint": "is the customer's account already live, or is onboarding still pending?"},
  {"name": "analyst_note", "type": "string",
   "hint": "the triaging analyst's own free-text remark on the alert, copied verbatim"},
  {"name": "verdict", "type": "enum",
   "values": ["same_party", "not_a_match", "insufficient_information"],
   "hint": "the adjudication this alert's own identifiers produce. Decide it STRICTLY from the two identifier types and values, the two dates of birth and the two places of birth -- never from the names, never from the nationalities and never from the analyst's note. Work through the five checks IN ORDER, stopping at the first that fires. (1) Both records carry a strong identifier OF THE SAME TYPE with the SAME value: 'same_party'. (2) Both carry one of the same type with DIFFERENT values: 'not_a_match'. (3) Otherwise, any comparable moderate identifier that DISAGREES: 'not_a_match'. (4) Otherwise, at least two comparable moderate identifiers agreeing and none disagreeing: 'same_party'. (5) Otherwise: 'insufficient_information'"},
  {"name": "deciding_identifier", "type": "enum",
   "values": ["passport_number", "national_id_number", "tax_reference", "date_of_birth", "place_of_birth", "date_of_birth_and_place_of_birth", "none"],
   "hint": "WHICH identifier produced that verdict. A strong identifier that matched or conflicted is named on its own. A single conflicting moderate identifier is named on its own. Two agreeing moderate identifiers are 'date_of_birth_and_place_of_birth'. When the verdict is 'insufficient_information' the answer is 'none' -- nothing on the file decided it"}
 ]
}
