{
 "fields": [
  {"name": "line_id", "type": "string",
   "hint": "the statement line identifier, verbatim"},
  {"name": "expense_category", "type": "string",
   "hint": "the expense category as written on the line, verbatim. This is a LABEL, not a classification -- it does not decide whether the line is billable"},
  {"name": "expense_class", "type": "enum",
   "values": ["routine_operating", "capital_improvement", "landlord_overhead", "leasing_cost"],
   "hint": "the structured expense classification on the line, verbatim. THIS is what decides whether the pool is billable, never the category name"},
  {"name": "pool_gross_usd", "type": "number",
   "hint": "the gross cost of this expense pool at the property for the reconciliation year, as a bare number without the currency"},
  {"name": "amortization_years", "type": "number",
   "hint": "the number of years this lease permits a capital item to be amortized over. Stated only on a capital improvement the lease permits amortizing; null on every other line -- return null rather than 0 or a guess"},
  {"name": "occupancy_sensitive", "type": "enum", "values": ["yes", "no"],
   "hint": "whether this expense varies with how much of the building is occupied, verbatim"},
  {"name": "building_occupancy_pct", "type": "number",
   "hint": "the building's average occupancy for the reconciliation year, as a bare number without the unit"},
  {"name": "building_area_sf", "type": "number",
   "hint": "the building's total rentable area in square feet, as a bare number without the unit"},
  {"name": "tenant_area_sf", "type": "number",
   "hint": "the tenant's rentable area at the START of the reconciliation year, in square feet, as a bare number without the unit"},
  {"name": "expansion_area_sf", "type": "number",
   "hint": "additional rentable area the tenant took mid-year, in square feet, as a bare number. Null when the line states there was no expansion this reconciliation year"},
  {"name": "expansion_month", "type": "number",
   "hint": "the month number (1-12) the mid-year expansion took effect. Null when there was no expansion"},
  {"name": "cap_type", "type": "enum", "values": ["none", "annual", "cumulative"],
   "hint": "the kind of cap this lease puts on the tenant's share of this line, verbatim"},
  {"name": "cap_pct", "type": "number",
   "hint": "the cap percentage, as a bare number without the unit. Null when cap_type is none"},
  {"name": "cap_basis_usd", "type": "number",
   "hint": "the dollar amount the cap is measured from -- last year's billed amount for an annual cap, the base year's for a cumulative one. As a bare number without the currency. Null when cap_type is none"},
  {"name": "cap_years", "type": "number",
   "hint": "the number of compounding periods since the base year. Stated only on a cumulative cap; null on an annual cap and null when there is no cap"},
  {"name": "billed_to_tenant_usd", "type": "number",
   "hint": "the amount the landlord actually put on this tenant's statement for this line, as a bare number without the currency"},
  {"name": "statement_status", "type": "enum", "values": ["draft", "issued"],
   "hint": "has this reconciliation statement already gone out to the tenant, or is it still a draft?"},
  {"name": "accountant_note", "type": "string",
   "hint": "the property accountant's own free-text note on this line, copied verbatim"},
  {"name": "permitted_amount_usd", "type": "number", "computed": true,
   "hint": "the amount the lease actually permits the landlord to bill THIS tenant for THIS line, in dollars to the cent, computed by you in four stages. STAGE 1, POOLABLE: expense_class landlord_overhead or leasing_cost -> 0. expense_class capital_improvement with amortization_years null -> 0; with amortization_years N -> pool_gross_usd divided by N, ONE annual instalment, not the whole cost. expense_class routine_operating -> pool_gross_usd in full, WHATEVER the category is called. STAGE 2, GROSS-UP: if occupancy_sensitive is yes AND building_occupancy_pct is below 95, multiply by 95 and divide by building_occupancy_pct; otherwise leave it alone -- never gross up a line whose occupancy_sensitive is no. STAGE 3, PRO RATA: multiply by the tenant's weighted share. With no expansion that share is tenant_area_sf / building_area_sf. With an expansion of A square feet in month M it is (tenant_area_sf + A * (13 - M) / 12) / building_area_sf. STAGE 4, CAP: if cap_type is annual the ceiling is cap_basis_usd * (1 + cap_pct/100); if cap_type is cumulative the ceiling is cap_basis_usd * (1 + cap_pct/100) raised to the power cap_years; if cap_type is none there is no ceiling. Take the LOWER of the stage-3 amount and the ceiling. Round to two decimals"},
  {"name": "line_ok", "type": "enum", "values": ["yes", "no"], "computed": true,
   "hint": "does billed_to_tenant_usd match permitted_amount_usd? Answer 'yes' when the two are within 1.00 US dollar of each other and 'no' otherwise. Decide this STRICTLY from the arithmetic -- never from the accountant's note and never from what the expense category is called"}
 ]
}
